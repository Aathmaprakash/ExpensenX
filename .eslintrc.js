module.exports = {
  root: true,
  extends: '@react-native',
  rules: {
    'prettier/prettier': 0,  // Disabling prettier/prettier rule
    // 'no-trailing-spaces': 'error',  // Enabling no-trailing-spaces rule
  },
};
